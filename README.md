# 📚 PaperWorlds

Платформа для публикации и чтения фанфиков (произведений).

## 🚀 Установка

### 1. Клонировать репозиторий
```bash
git clone <repository-url>
cd PaperWorlds
```

### 2. Создать виртуальное окружение
```bash
python -m venv venv
```

### 3. Активировать окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 4. Установить зависимости
```bash
pip install -r requirements.txt
```

Если файла `requirements.txt` нет, установи Django:
```bash
pip install django
```

### 5. Применить миграции
```bash
python manage.py migrate
```

### 6. Создать суперпользователя (опционально)
```bash
python manage.py createsuperuser
```

### 7. Запустить сервер
```bash
python manage.py runserver
```

Открой в браузере: http://127.0.0.1:8000/

---

## 📁 Структура проекта

```
PaperWorlds/
├── paperwolds/          # Настройки Django проекта
│   ├── __init__.py
│   ├── settings.py      # Настройки проекта
│   ├── urls.py          # Главный URL-конфигуратор
│   ├── wsgi.py
│   └── asgi.py
├── works/               # Приложение для произведений
│   ├── models.py        # Модель Work
│   ├── views.py         # Представления (work_list, work_detail)
│   ├── urls.py          # URL-маршруты приложения
│   ├── admin.py         # Настройки админ-панели
│   └── migrations/      # Миграции БД
├── db.sqlite3           # База данных (не коммитится)
├── manage.py            # Управляющий скрипт Django
├── .gitignore           # Игнорируемые файлы
└── README.md            # Этот файл
```

---

## 🛠️ Технологии

- **Python** 3.x
- **Django** 6.0
- **SQLite** (база данных)

---

## 📝 Функционал

### ✅ Реализовано:
- [x] Базовая структура Django проекта
- [x] Модель `Work` (произведение) с полями:
  - `title` - название
  - `slug` - URL-слаг
  - `author` - автор (ForeignKey к User)
  - `description` - краткое описание (аннотация)
  - `status` - статус (Draft, Published, Completed)
  - `created` - дата создания
  - `updated` - дата обновления
- [x] Кастомный менеджер `PublishedManager` для фильтрации опубликованных работ
- [x] Представления:
  - `work_list` - список всех опубликованных произведений (с пагинацией)
  - `work_detail` - детальная страница произведения
- [x] Пагинация (8 произведений на страницу)
- [x] Индексы в БД для оптимизации

### 🚧 В разработке:
- [ ] Шаблоны (templates) для отображения
- [ ] Поиск по произведениям
- [ ] Категории и теги
- [ ] Комментарии к произведениям
- [ ] Рейтинги и оценки
- [ ] Профили пользователей
- [ ] Загрузка файлов (тексты произведений)

---

## 🎯 Модель Work

### Статусы:
- **Draft (DF)** - черновик (не опубликован)
- **Published (PB)** - опубликован
- **Completed (CM)** - завершён

### Менеджеры:
- `objects` - менеджер по умолчанию (все произведения)
- `published` - только опубликованные произведения

### Пример использования:
```python
# Получить все опубликованные произведения
works = Work.published.all()

# Получить произведение по ID
work = Work.published.get(id=1)

# Получить все произведения автора
user_works = user.works.all()
```

---

## 🔧 Команды Django

### Создать миграции:
```bash
python manage.py makemigrations
```

### Применить миграции:
```bash
python manage.py migrate
```

### Запустить сервер:
```bash
python manage.py runserver
```

### Создать суперпользователя:
```bash
python manage.py createsuperuser
```

### Открыть Django shell:
```bash
python manage.py shell
```

---

## 📚 URL-маршруты

- `/` - главная страница (список произведений)
- `/works/<id>/` - детальная страница произведения
- `/admin/` - админ-панель Django

---

## 🐛 Известные проблемы

- Шаблоны еще не созданы (нужно создать `templates/works/work/list.html` и `detail.html`)
- Статические файлы (CSS, JS) не настроены
- Нет формы для создания произведений

---

## 📖 Документация

- [Django Documentation](https://docs.djangoproject.com/)
- [Django Tutorial](https://docs.djangoproject.com/en/stable/intro/tutorial01/)

---

## 👤 Автор

Проект создан в рамках изучения Django.

---

## 📄 Лицензия

Это учебный проект.

---

**Удачи в разработке! 🚀**

