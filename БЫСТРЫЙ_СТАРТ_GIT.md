# ⚡ БЫСТРЫЙ СТАРТ: GIT ДЛЯ PAPERWORLDS

**Время:** 15-20 минут  
**Цель:** Инициализировать Git и закоммитить проект

---

## 🎯 ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС

### **ШАГ 1: Открой терминал в папке PaperWorlds**

```bash
cd PaperWorlds
```

### **ШАГ 2: Инициализируй Git**

```bash
git init
```

**Результат:** `Initialized empty Git repository in ...`

### **ШАГ 3: Проверь статус**

```bash
git status
```

**Что увидишь:** Список файлов, которые не отслеживаются Git

### **ШАГ 4: Добавь все файлы**

```bash
git add .
```

**Что делает:** Добавляет все файлы в staging area (кроме тех, что в .gitignore)

### **ШАГ 5: Проверь, что добавилось**

```bash
git status
```

**Что увидишь:** Файлы помечены как "Changes to be committed"

### **ШАГ 6: Создай первый коммит**

```bash
git commit -m "Initial commit: PaperWorlds Django project

- Django проект настроен
- Приложение works создано
- Модель Work с полями и статусами
- Views: work_list и work_detail
- Пагинация реализована
- Кастомный менеджер PublishedManager
- .gitignore настроен"
```

**Результат:** `[main (root-commit) <hash>] Initial commit...`

### **ШАГ 7: Проверь историю**

```bash
git log
```

**Или краткий вид:**
```bash
git log --oneline
```

---

## ✅ ПРОВЕРКА

Выполни команды и проверь результат:

```bash
# Проверить статус (должно быть "nothing to commit")
git status

# Посмотреть историю (должен быть 1 коммит)
git log --oneline

# Посмотреть, какие файлы отслеживаются
git ls-files
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После первого коммита:

1. **Создай README.md** (уже создан в проекте)
2. **Закоммить README:**
   ```bash
   git add README.md
   git commit -m "Add README.md with project description"
   ```

3. **Попрактикуйся с ветками:**
   ```bash
   # Создать ветку
   git checkout -b feature/add-templates
   
   # Сделать изменения (например, создать templates)
   # ...
   
   # Закоммитить
   git add .
   git commit -m "Add templates for work list and detail"
   
   # Вернуться на main
   git checkout main
   
   # Слить ветку
   git merge feature/add-templates
   ```

---

## 📚 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Статус
git status

# Добавить файлы
git add .              # Все файлы
git add file.py        # Конкретный файл

# Коммит
git commit -m "Сообщение"

# История
git log                # Полная история
git log --oneline      # Краткая история
git log --graph        # С графиком веток

# Ветки
git branch             # Список веток
git checkout -b name    # Создать и переключиться
git checkout main       # Переключиться на main
git merge branch        # Слить ветку

# Просмотр изменений
git diff               # Изменения в рабочей директории
git diff --staged      # Изменения в staging area
```

---

## 🎉 ГОТОВО!

Ты инициализировал Git и закоммитил проект. Теперь все изменения отслеживаются!

**Следующий шаг:** Изучи `ПЛАН_РАБОТЫ_С_GIT.md` для более глубокой практики.

