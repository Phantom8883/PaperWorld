# 🎯 ПЛАН РАБОТЫ С GIT: ЗАКРЕПЛЕНИЕ МАТЕРИАЛОВ

**Дата:** Пятница  
**Цель:** Инициализировать Git, закоммитить текущий прогресс, закрепить знания  
**Время:** 1-2 часа

---

## ✅ ЧТО УЖЕ ЕСТЬ В ПРОЕКТЕ

### Структура PaperWorlds:
- ✅ Django проект настроен
- ✅ Приложение `works` (произведения)
- ✅ Модели созданы (миграции есть)
- ✅ `.gitignore` файл создан
- ⚠️ Git репозиторий НЕ инициализирован

---

## 🎯 ЧТО НУЖНО РЕАЛИЗОВАТЬ СЕГОДНЯ

### **1. Инициализация Git (10 мин)**
- [ ] Инициализировать Git репозиторий
- [ ] Проверить статус
- [ ] Настроить Git (если еще не настроен)

### **2. Первый коммит (20 мин)**
- [ ] Добавить все файлы проекта
- [ ] Создать первый коммит с описанием
- [ ] Проверить историю коммитов

### **3. Создание README.md (30 мин)**
- [ ] Описать проект PaperWorlds
- [ ] Добавить инструкции по установке
- [ ] Описать структуру проекта
- [ ] Добавить список технологий

### **4. Работа с ветками (30 мин)**
- [ ] Создать ветку для новой функции
- [ ] Сделать изменения
- [ ] Закоммитить в ветку
- [ ] Слить ветку в main

### **5. Практика Git команд (30 мин)**
- [ ] `git status` - проверка статуса
- [ ] `git add` - добавление файлов
- [ ] `git commit` - создание коммитов
- [ ] `git log` - просмотр истории
- [ ] `git branch` - работа с ветками
- [ ] `git checkout` - переключение веток
- [ ] `git merge` - слияние веток
- [ ] `git diff` - просмотр изменений

---

## 📋 ПОШАГОВЫЙ ПЛАН

### **ШАГ 1: Проверка и настройка Git (5 мин)**

```bash
# Проверить версию Git
git --version

# Проверить настройки (если нужно настроить)
git config --global user.name "Твоё имя"
git config --global user.email "твой@email.com"

# Перейти в папку проекта
cd PaperWorlds
```

### **ШАГ 2: Инициализация репозитория (5 мин)**

```bash
# Инициализировать Git
git init

# Проверить статус
git status
```

### **ШАГ 3: Первый коммит (15 мин)**

```bash
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Проверить, что добавилось
git status

# Создать первый коммит
git commit -m "Initial commit: PaperWorlds Django project

- Django проект настроен
- Приложение works создано
- Модели и миграции готовы
- .gitignore настроен"
```

### **ШАГ 4: Создание README.md (30 мин)**

Создать файл `README.md` с описанием проекта:

```markdown
# 📚 PaperWorlds

Платформа для публикации и чтения фанфиков (произведений).

## 🚀 Установка

1. Клонировать репозиторий
2. Создать виртуальное окружение: `python -m venv venv`
3. Активировать окружение: `venv\Scripts\activate` (Windows)
4. Установить зависимости: `pip install -r requirements.txt`
5. Применить миграции: `python manage.py migrate`
6. Запустить сервер: `python manage.py runserver`

## 📁 Структура проекта

- `paperwolds/` - настройки Django проекта
- `works/` - приложение для произведений
- `db.sqlite3` - база данных (не коммитится)

## 🛠️ Технологии

- Python 3.x
- Django 6.0
- SQLite

## 📝 Функционал

- [x] Базовая структура проекта
- [x] Модели для произведений
- [ ] Список произведений
- [ ] Детальная страница произведения
- [ ] Поиск по произведениям
- [ ] Категории и теги
```

### **ШАГ 5: Коммит README (5 мин)**

```bash
# Добавить README
git add README.md

# Закоммитить
git commit -m "Add README.md with project description and setup instructions"
```

### **ШАГ 6: Работа с ветками (30 мин)**

```bash
# Создать ветку для новой функции
git checkout -b feature/add-views

# Сделать изменения (например, добавить views)
# ... работа с кодом ...

# Добавить изменения
git add .

# Закоммитить в ветку
git commit -m "Add views for works list and detail"

# Переключиться на main
git checkout main

# Слить ветку
git merge feature/add-views

# Удалить ветку (опционально)
git branch -d feature/add-views
```

### **ШАГ 7: Практика команд (30 мин)**

```bash
# Просмотр истории
git log
git log --oneline
git log --graph --oneline --all

# Просмотр изменений
git diff
git diff HEAD~1  # Изменения в последнем коммите

# Просмотр статуса
git status

# Просмотр веток
git branch
git branch -a  # Все ветки

# Просмотр конкретного коммита
git show <commit-hash>
```

---

## 📚 ЧТО ИЗУЧИТЬ (теория)

### **Основные понятия:**
1. **Репозиторий** - папка с проектом, где Git отслеживает изменения
2. **Коммит** - снимок состояния проекта в определённый момент
3. **Ветка (branch)** - отдельная линия разработки
4. **Слияние (merge)** - объединение изменений из разных веток
5. **Статус (status)** - текущее состояние файлов

### **Рабочий процесс:**
1. Вносишь изменения в файлы
2. `git add` - добавляешь файлы в staging area
3. `git commit` - создаёшь коммит с описанием
4. `git push` - отправляешь на GitHub (если есть)

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕНИЯ

### Git основы:
- [ ] Git установлен и настроен
- [ ] Репозиторий инициализирован
- [ ] Первый коммит создан
- [ ] README.md создан и закоммичен

### Практика команд:
- [ ] `git status` - использовал
- [ ] `git add` - использовал
- [ ] `git commit` - использовал
- [ ] `git log` - использовал
- [ ] `git branch` - использовал
- [ ] `git checkout` - использовал
- [ ] `git merge` - использовал
- [ ] `git diff` - использовал

### Работа с ветками:
- [ ] Создал ветку
- [ ] Сделал изменения в ветке
- [ ] Закоммитил в ветку
- [ ] Слил ветку в main

---

## 🎯 ДОПОЛНИТЕЛЬНЫЕ ЗАДАЧИ (если останется время)

### **1. Создание .gitignore (уже есть, но проверить)**
- [ ] Убедиться, что все нужные файлы игнорируются
- [ ] Добавить дополнительные паттерны, если нужно

### **2. Подключение к GitHub (опционально)**
- [ ] Создать репозиторий на GitHub
- [ ] Добавить remote: `git remote add origin <url>`
- [ ] Отправить код: `git push -u origin main`

### **3. Создание тегов (опционально)**
- [ ] Создать тег для версии: `git tag v1.0.0`
- [ ] Отправить теги: `git push --tags`

---

## 💡 СОВЕТЫ

### **1. Хорошие коммиты:**
- Один коммит = одна логическая задача
- Описательные сообщения коммитов
- Не коммитить временные файлы

### **2. Работа с ветками:**
- Создавай ветку для каждой новой функции
- Называй ветки понятно: `feature/add-search`, `fix/bug-in-views`
- Сливай ветки после завершения работы

### **3. Регулярность:**
- Делай коммиты часто (после каждой логической задачи)
- Не накапливай много изменений в одном коммите
- Проверяй статус перед коммитом

---

## 🚀 БЫСТРЫЙ СТАРТ

**Что делать прямо сейчас:**

1. **Открой терминал в папке PaperWorlds**
2. **Выполни команды:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: PaperWorlds project"
   ```
3. **Создай README.md** (используй шаблон выше)
4. **Закоммить README:**
   ```bash
   git add README.md
   git commit -m "Add README.md"
   ```
5. **Попрактикуйся с ветками:**
   ```bash
   git checkout -b feature/test
   # Сделай какие-то изменения
   git add .
   git commit -m "Test commit in branch"
   git checkout main
   git merge feature/test
   ```

**Готово! Ты закрепил работу с Git! 🎉**

---

## 📖 РЕСУРСЫ

- **Официальная документация:** https://git-scm.com/doc
- **Интерактивный туториал:** https://learngitbranching.js.org/
- **GitHub Guides:** https://guides.github.com/

---

**Удачи! 💪**

